apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: accounts
  labels:
    app: accounts
spec:
  to:
    kind: Service
    name: accounts
  port:
    targetPort: http
  tls:
    termination: edge
  wildcardPolicy: None